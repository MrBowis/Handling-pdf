Maneja los endpoint de comunicación con APIs, como subir archivos o enviar datos al backend. ejemplo:


endpoints que ofrece la api

EJEMPLO: 

  getMovieDetails: async (movieId) => {
    const url = `${apiConfig.BASE_URL}/movie/${movieId}?api_key=${apiConfig.API_KEY}&language=${api.languaje}&append_to_response=credits,videos,images,recommendations,similar,keywords,watch/providers,release_dates,reviews,external_ids`;
    return await api.get(url);
  },